# Copie para e2e/.env
E2E_BASE_URL=http://localhost
E2E_TEST_EMAIL=teste@exemplo.com
E2E_TEST_PASSWORD=sua-senha-de-teste

# Timeout de execucao: padrao 10s (EXEC_TIMEOUT_S no .env da raiz / docker-compose).
# Nao aumente para os E2E — o cenario de timeout valida exatamente 10s.
